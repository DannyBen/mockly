require 'mockly/version'
begin
  require 'debug'
rescue LoadError
end

title   "Mockly Runfile"
summary "Runfile tasks for building the Mockly gem"

import_gem 'runfile-tasks/gem'
import_gem 'runfile-tasks/docker', image: 'dannyben/mockly', version: Mockly::VERSION
import 'debug'
